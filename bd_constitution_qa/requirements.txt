# Bangladesh Constitution QA System — Requirements (cleaned, minimal)
# Tested working combo on Colab T4 (Python 3.12)
# NOTE: torch intentionally NOT pinned — use whatever Colab/your env already
# has preinstalled (matches CUDA build). Pinning torch caused version-hell
# with torchvision/torchao earlier.

# ── Core ML ─────────────────────────────────────────────────────────────────
transformers==4.46.3
datasets==2.19.0
tokenizers==0.20.3
accelerate==1.0.1
huggingface_hub==0.26.2
evaluate==0.4.1
sentencepiece==0.2.0

# ── Retrieval (used in UI for question matching) ────────────────────────────
scikit-learn==1.4.0

# ── Frontend ─────────────────────────────────────────────────────────────────
streamlit==1.35.0

# ── Data processing ──────────────────────────────────────────────────────────
numpy==1.26.0
tqdm==4.66.0

# ── Dev utilities ────────────────────────────────────────────────────────────
loguru==0.7.2
